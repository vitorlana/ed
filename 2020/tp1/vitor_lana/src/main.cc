#include <cstdio> // em C substituir por #include <stdio.h>
#include "headers/foo.h" //um exemplo de como incluir cabeçalhos de outros arquivos criados

int main() {
  int num_frotas;
  scanf("%d",&num_frotas);
  int i;
  for(i=0; i < num_frotas; i++){
    int id_nave;
    scanf("%d",&id_nave);
    //desenvolver o código para inserção correta das naves que são lidas
  }
  int operacao;
  while(scanf("%d", &operacao) != EOF) {
    //desenvolver o código relacionado as operações a serem realizadas
  }
  return 0;
}
