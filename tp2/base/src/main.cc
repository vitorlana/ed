#include <cstdio> // em C substituir por #include <stdio.h>
#include "foo.h"

int main() {
  int t;
  int p;
  int c;
  while(scanf("%d %d %d", &t, &p, &c) != EOF) {
      return 1;
  }
  return 0;
}
